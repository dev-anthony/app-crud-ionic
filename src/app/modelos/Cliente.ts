export class Cliente {

    id: string;
    nombre: string;
    apellido: string;
    telefono: string;
    correo: string;

    // constructor(public id: string, public nombre: string, public apellido: string, public telefono: string, public correo: string) {
    // }
}